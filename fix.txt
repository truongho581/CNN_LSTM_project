git gc --prune=now --aggressive
Remove-Item -Recurse -Force .\.venv
py -3.10 -m venv .venv

pip install --upgrade pip
pip install tensorflow
pip install obspy scipy matplotlib numpy
pip install scikit-learn
pip install seaborn

